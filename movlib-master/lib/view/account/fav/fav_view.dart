import 'package:flutter/material.dart';

class FavView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Você ainda não adicionou nenhum filme'),
    );
  }
}
